import type { Room, Socket } from '../../common/types';
import React, { useState } from 'react';

type Props = {};

export default function WaitingSubview(props: Props): React.Node {
  return <div>Your codemaster is making clues, sit tight!</div>;
}
